apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-{{ "user-service" }}
spec:
  replicas: {{ .Values.replicaCount | int }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-{{ "user-service" }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-{{ "user-service" }}
    spec:
      containers:
        - name: {{ .Chart.Name }}-{{ "user-service" }}
          image: "{{ .Values.image.repository }}/{{ "user-service" }}:latest"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ 8080 | int }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-{{ "user-service" }}
spec:
  selector:
    app: {{ .Chart.Name }}-{{ "user-service" }}
  ports:
    - protocol: TCP
      port: {{ 8080 | int }}
      targetPort: {{ 8080 | int }}
