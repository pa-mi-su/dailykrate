apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-{{ "data-service" }}
spec:
  replicas: {{ .Values.replicaCount | int }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-{{ "data-service" }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-{{ "data-service" }}
    spec:
      containers:
        - name: {{ .Chart.Name }}-{{ "data-service" }}
          image: "{{ .Values.image.repository }}/{{ "data-service" }}:latest"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ 8080 | int }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-{{ "data-service" }}
spec:
  selector:
    app: {{ .Chart.Name }}-{{ "data-service" }}
  ports:
    - protocol: TCP
      port: {{ 8080 | int }}
      targetPort: {{ 8080 | int }}
